{
  "version": 2,
  "rewrites": [{ "source": "/(.*)", "destination": "/" }] ,
  "builds": [
    {
      "src": "backend/package.json",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "backend/$1"
    }
  ]
}
